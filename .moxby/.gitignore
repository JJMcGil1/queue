# Auto-generated by Moxby. Per-machine codebase comprehension state.
# Edit Moxby settings, not this file — it will be rewritten on toggle.
#
# `comprehension.json` carries a `repo_id` hash of the remote URL +
# canonical local path, so two collaborators compute different ids on
# the same repo. Toggle state is also local.
comprehension.json
